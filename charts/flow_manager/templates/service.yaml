apiVersion: v1
kind: Service
metadata:
  name: {{ include "flow_manager.fullname" . }}
  labels:
    {{- include "flow_manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flow_manager.selectorLabels" . | nindent 4 }}
