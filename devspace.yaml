version: v2beta1
name: stalker

vars:
  STALKER_ENVIRONMENT: dev

imports:
  - path: devspace.dev.yaml
    enabled: $(is_equal ${STALKER_ENVIRONMENT} "dev")
  - path: devspace.prod.yaml
    enabled: $(is_equal ${STALKER_ENVIRONMENT} "prod")
  - path: devspace.base.yaml

pipelines:
  build:
    run: build_images ui flow-manager orchestrator python-job-base nuclei-job-base

  deploy:
    run: |-
      build_images ui flow-manager orchestrator python-job-base nuclei-job-base
      create_deployments mongo ui flow-manager kafka orchestrator-service-account orchestrator --sequential

  dev:
    run: |-
      create_deployments namespaces

      # Kafka's keystore and keystore password
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks --from-file=kafka.truststore.jks=./queue/kafka.server.truststore.jks --from-file=kafka.keystore.jks=./queue/kafka-0.keystore.jks -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks-password --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks-password --from-literal=password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=keystore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=truststore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=key-password=${KAFKA_KEYSTORE_PASSWORD} -n ${DEVSPACE_NAMESPACE}

      build_images ui flow-manager orchestrator python-job-base nuclei-job-base cron-service
      create_deployments mongo kafka --sequential

      # Start mongo and kafka
      start_dev mongo kafka

      # Wait for kafka and mongo to be ready
      wait_pod --label-selector app.kubernetes.io/component=mongodb --timeout 300
      wait_pod --label-selector app.kubernetes.io/instance=kafka --timeout 300

      create_deployments ui flow-manager orchestrator-service-account orchestrator --sequential

      # Wait for services before applying network policies
      wait_pod --label-selector app.kubernetes.io/component=ui --timeout 300
      wait_pod --label-selector app.kubernetes.io/component=flow-manager --timeout 300
      wait_pod --label-selector app.kubernetes.io/component=orchestrator --timeout 300


      create_deployments network-policies
      create_deployments resource-quotas

      # Start microservices + ui
      start_dev orchestrator flow-manager ui

      create_deployments cron-service

      wait_pod --label-selector app.kubernetes.io/component=cron-service --timeout 300

      start_dev cron-service

  flow-manager-e2e:
    flags:
      - name: watch
        short: w
        type: bool
    run: |-
      create_deployments namespaces

      # Kafka's keystore and keystore password
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks --from-file=kafka.truststore.jks=./queue/kafka.server.truststore.jks --from-file=kafka.keystore.jks=./queue/kafka-0.keystore.jks -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks-password --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks-password --from-literal=password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=keystore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=truststore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=key-password=${KAFKA_KEYSTORE_PASSWORD} -n ${DEVSPACE_NAMESPACE}

      build_images flow-manager-e2e orchestrator python-job-base nuclei-job-base
      create_deployments mongo kafka orchestrator-service-account orchestrator flow-manager-e2e --sequential

      # Wait for kafka and mongo to be ready
      wait_pod --label-selector app.kubernetes.io/component=mongodb --timeout 300
      wait_pod --label-selector app.kubernetes.io/instance=kafka --timeout 300

      # Run tests
      if [ $(get_flag "watch") == "true" ]; 
      then
        echo "Running tests in watch mode."
        start_dev flow-manager-e2e mongo
        exec_container --label-selector app.kubernetes.io/component=flow-manager-e2e -n stalker -- /bin/sh -c 'npm run test:e2e:watch'
      else
        echo "Running tests in ci mode."
        start_dev flow-manager-e2e
        exec_container --label-selector app.kubernetes.io/component=flow-manager-e2e -n stalker -- /bin/sh -c 'npm run test:e2e:cicd' 2>&1
      fi
      stop_dev --all
  purge:
    run: |-
      stop_dev --all
      purge_deployments mongo ui flow-manager flow-manager-e2e kafka orchestrator-service-account orchestrator cron-service ui-prod --sequential

  build-e2e:
    run: build_images ui flow-manager-e2e orchestrator python-job-base nuclei-job-base

  flow-manager-e2e-cicd-deploy:
    run: |-
      # Running the build pipeline is required before running this
      create_deployments namespaces

      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks --from-file=kafka.truststore.jks=./queue/kafka.server.truststore.jks --from-file=kafka.keystore.jks=./queue/kafka-0.keystore.jks -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks-password --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks-password --from-literal=password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=keystore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=truststore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=key-password=${KAFKA_KEYSTORE_PASSWORD} -n ${DEVSPACE_NAMESPACE}

      create_deployments mongo kafka orchestrator-service-account orchestrator flow-manager-e2e --sequential

      # Wait for kafka and mongo to be ready
      wait_pod --label-selector app.kubernetes.io/component=mongodb --timeout 300
      wait_pod --label-selector app.kubernetes.io/instance=kafka --timeout 300

  flow-manager-e2e-cicd-run:
    run: |-
      # Running flow-manager-e2e-cicd-deploy pipeline is required before running this
      start_dev flow-manager-e2e
      exec_container --label-selector app.kubernetes.io/component=flow-manager-e2e -n stalker -- /bin/sh -c 'npm run test:e2e:cicd'
      stop_dev --all

  prod:
    run: |-
      create_deployments namespaces

      # Kafka's keystore and keystore password
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks --from-file=kafka.truststore.jks=./queue/kafka.server.truststore.jks --from-file=kafka.keystore.jks=./queue/kafka-0.keystore.jks -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} delete secret kafka-jks-password --ignore-not-found -n ${DEVSPACE_NAMESPACE}
      ${DEVSPACE_KUBECTL_EXECUTABLE} create secret generic kafka-jks-password --from-literal=password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=keystore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=truststore-password=${KAFKA_KEYSTORE_PASSWORD} --from-literal=key-password=${KAFKA_KEYSTORE_PASSWORD} -n ${DEVSPACE_NAMESPACE}

      build_images ui flow-manager orchestrator python-job-base nuclei-job-base cron-service
      create_deployments mongo kafka --sequential

      # Start mongo and kafka
      start_dev mongo-prod kafka

      # Wait for kafka and mongo to be ready
      wait_pod --label-selector app.kubernetes.io/component=mongodb --timeout 300
      wait_pod --label-selector app.kubernetes.io/instance=kafka --timeout 300

      create_deployments ui-prod flow-manager orchestrator-service-account orchestrator --sequential

      # Wait for services before applying network policies
      wait_pod --label-selector app.kubernetes.io/component=ui-prod --timeout 300
      wait_pod --label-selector app.kubernetes.io/component=flow-manager --timeout 300
      wait_pod --label-selector app.kubernetes.io/component=orchestrator --timeout 300


      create_deployments network-policies
      create_deployments resource-quotas

      # Start microservices + ui
      start_dev orchestrator-prod flow-manager-prod ui-prod

      create_deployments cron-service

      wait_pod --label-selector app.kubernetes.io/component=cron-service --timeout 300

      start_dev cron-service-prod

images:
  flow-manager:
    image: flow_manager
    dockerfile: flow_manager/${DOCKERFILE_NAME}
    context: flow_manager
    rebuildStrategy: default

  flow-manager-e2e:
    image: flow_manager_e2e
    dockerfile: flow_manager/Dockerfile.cicd
    context: flow_manager
    rebuildStrategy: always

  cron-service:
    image: cron_service
    dockerfile: cron_service/${DOCKERFILE_NAME}
    context: cron_service
    rebuildStrategy: default

  ui:
    image: ui
    dockerfile: stalker_ui/${DOCKERFILE_NAME}
    context: stalker_ui
    rebuildStrategy: default

  orchestrator:
    image: orchestrator
    dockerfile: orchestrator/${DOCKERFILE_NAME}
    context: orchestrator
    rebuildStrategy: default

  python-job-base:
    image: python-job-base
    dockerfile: jobs/job-base-images/python/Dockerfile
    tags:
      - "latest"
      - "v1"
    context: jobs/job-base-images/python
    rebuildStrategy: always

  nuclei-job-base:
    image: nuclei-job-base
    dockerfile: jobs/job-base-images/python/Dockerfile.nuclei
    tags:
      - "latest"
      - "v1"
    context: jobs/job-base-images/python/
    rebuildStrategy: always

deployments:
  flow-manager:
    helm:
      values:
        containers:
          - env:
              - name: MONGO_ADDRESS
                value: ${FM_MONGO_ADDRESS}
              - name: MONGO_DATABASE_NAME
                value: ${FM_MONGO_DATABASE_NAME}
              - name: FM_JWT_SECRET
                value: ${FM_JWT_SECRET}
              - name: FM_REFRESH_SECRET
                value: ${FM_REFRESH_SECRET}
              - name: KAFKA_URI
                value: ${KAFKA_URI}
              - name: STALKER_URL
                value: ${STALKER_URL}
              - name: MONGO_REPLICA_SET_NAME
                value: ${MONGO_REPLICA_SET_NAME}
              - name: FM_ENVIRONMENT
                value: ${FM_ENVIRONMENT}
              - name: STALKER_CRON_API_TOKEN
                value: ${STALKER_CRON_API_TOKEN}
              - name: SECRET_PUBLIC_RSA_KEY
                value: ${SECRET_PUBLIC_RSA_KEY}
              - name: FM_MONGO_KEY_PASSWORD
                value: ${FM_MONGO_KEY_PASSWORD}
              - name: FM_KAFKA_KEY_PASSWORD
                value: ${FM_KAFKA_KEY_PASSWORD}
              - name: STALKER_VERSION
                value: ${STALKER_VERSION}

            image: flow_manager
            name: flow-manager-container

        service:
          ports:
            - containerPort: 3000
              port: 3000
              protocol: TCP

  flow-manager-e2e:
    helm:
      values:
        containers:
          - image: flow_manager_e2e
            name: flow-manager-container-e2e
            env:
              - name: MONGO_ADDRESS
                value: ${FM_MONGO_ADDRESS_E2E}
              - name: MONGO_DATABASE_NAME
                # value: stalker-flow-manager-e2e
                value: ${FM_MONGO_DATABASE_NAME}
              - name: FM_JWT_SECRET
                value: ${FM_JWT_SECRET}
              - name: FM_REFRESH_SECRET
                value: ${FM_REFRESH_SECRET}
              - name: KAFKA_URI
                value: ${KAFKA_URI}
              - name: FEATURE_ORCHESTRATOR_ENABLED
                value: "true"
              - name: STALKER_URL
                value: ${STALKER_URL}
              - name: MONGO_REPLICA_SET_NAME
                value: ${MONGO_REPLICA_SET_NAME}
              - name: FM_ENVIRONMENT
                value: "tests"
              - name: STALKER_CRON_API_TOKEN
                value: ${STALKER_CRON_API_TOKEN}
              - name: SECRET_PUBLIC_RSA_KEY
                value: ${SECRET_PUBLIC_RSA_KEY}
              - name: FM_MONGO_KEY_PASSWORD
                value: ${FM_MONGO_KEY_PASSWORD}
              - name: FM_KAFKA_KEY_PASSWORD
                value: ${FM_KAFKA_KEY_PASSWORD}

        service:
          ports:
            - containerPort: 3000
              port: 3000
              protocol: TCP

  cron-service:
    helm:
      values:
        containers:
          - env:
              - name: MONGO_ADDRESS
                value: ${CRON_SERVICE_MONGO_ADDRESS}
              - name: MONGO_DATABASE_NAME
                value: ${FM_MONGO_DATABASE_NAME}
              - name: MONGO_REPLICA_SET_NAME
                value: ${MONGO_REPLICA_SET_NAME}
              - name: CRON_SERVICE_ENVIRONMENT
                value: ${FM_ENVIRONMENT} # Follows the same environment as FM since they work together
              - name: FM_URL
                value: ${FM_URL}
              - name: STALKER_CRON_API_TOKEN
                value: ${STALKER_CRON_API_TOKEN}
              - name: CRON_SERVICE_MONGO_KEY_PASSWORD
                value: ${CRON_SERVICE_MONGO_KEY_PASSWORD}

            image: cron_service
            name: cron-service-container

        service:
          ports:
            - containerPort: 3000
              port: 3001
              protocol: TCP

  ui:
    helm:
      values:
        containers:
          - env:

            image: ui
            name: ui-container

        service:
          ports:
            - containerPort: 4200
              port: 4200
              protocol: TCP

  ui-prod:
    helm:
      values:
        containers:
          - env: []
            image: ui
            name: ui-container

        service:
          ports:
            - containerPort: 80
              port: 8000
              protocol: TCP

  mongo: # volumes location: /var/lib/docker/volumes/minikube/_data/hostpath-provisioner/stalker/
    helm:
      chart:
        name: mongodb
        repo: https://charts.bitnami.com/bitnami
        version: "13.9.1"
      values:
        architecture: replicaset
        replicaCount: ${MONGO_REPLICA_SET_COUNT}
        arbiter:
          enabled: false
        externalAccess.enabled: true
        externalAccess.service.type: LoadBalancer
        externalAccess.service.port: 27017
        externalAccess.autoDiscovery.enabled: true
        serviceAccount.create: true
        rbac.create: true
        tls:
          enabled: true
          mode: requireTLS
          autoGenerated: false
          caCert: ${MONGO_CA_CRT}
          caKey: ${MONGO_CA_KEY}
          mTls:
            enabled: true
        service:
          ports:
            mongodb: 27017
        auth:
          rootPassword: ${MONGO_ROOT_PASSWORD}
        initdbScripts:
          init-db.js: |
            db = db.getSiblingDB("stalker");
            db.createCollection("cronsubscriptions");
            var cronRole = {
              role: "cronrole",
              privileges: [
                {
                  resource: {
                    db: "stalker",
                    collection: "cronsubscriptions"
                  },
                  actions: ["find"]
                }
              ],
              roles: []
            };
            db.createRole(cronRole);
            var cronUser = { user: "${MONGO_CRON_SERVICE_USER}", pwd: "${MONGO_CRON_SERVICE_PASSWORD}", roles: ["cronrole"] };
            db.createUser(cronUser);
            var flowRole = { 
              role: "flowrole", 
              privileges: [ 
                {  
                  resource: {
                    db:"stalker", 
                    collection:"" 
                  }, 
                  actions: ["find", "insert", "remove", "update", "createCollection", "changeStream", "createIndex", "convertToCapped"] 
                }
              ], 
              roles:[]
            }
            db.createRole(flowRole);
            var flowUser = { user: "${MONGO_FM_USER}", pwd: "${MONGO_FM_PASSWORD}", roles: ["flowrole"] };
            db.createUser(flowUser);

            // The next one is used for e2e testing
            var flowRole = { 
              role: "flowrolee2e", 
              privileges: [ 
                {  
                  resource: {
                    db:"stalker", 
                    collection:"" 
                  }, 
                  actions: ["find", "insert", "remove", "update", "createCollection", "changeStream", "createIndex", "dropCollection", "convertToCapped"] 
                }
              ], 
              roles:[]
            }
            db.createRole(flowRole);
            var flowUser = { user: "${MONGO_FM_USER_E2E}", pwd: "${MONGO_FM_PASSWORD}", roles: ["flowrolee2e"] };
            db.createUser(flowUser);

  kafka:
    helm:
      chart:
        name: kafka
        repo: https://charts.bitnami.com/bitnami
        version: "26.0.0"
      values:
        # The mapping rules extract the CN part of the certificate to be used as the username in ACL rules
        # kafka as a super user is the account used for provisioning and for inter-kafka communications.
        # Other accounts have minimal rights defined in "extraProvisioningCommands"
        extraConfig: |
          authorizer.class.name = org.apache.kafka.metadata.authorizer.StandardAuthorizer
          ssl.principal.mapping.rules = RULE:^.*OU=Stalker Kafka Clients.*,CN=(.*?)(,.*)?$/$1/,RULE:^CN=(.*?),OU=Stalker Kafka.*$/$1/,DEFAULT
          super.users = User:kafka
        listeners:
          client:
            name: CLIENT
            containerPort: 9092
            protocol: SSL
            sslClientAuth: "required"
          controller:
            name: CONTROLLER
            containerPort: 9093
            protocol: SSL
            sslClientAuth: "required"
          interbroker:
            name: INTERNAL
            containerPort: 9094
            protocol: SSL
            sslClientAuth: "required"
        tls:
          passwordsSecret: kafka-jks-password
          existingSecret: kafka-jks
          keystorePassword: ${KAFKA_KEYSTORE_PASSWORD}
          truststorePassword: ${KAFKA_KEYSTORE_PASSWORD}
          endpointIdentificationAlgorithm: "https"
        extraEnvVars:
          - name: ALLOW_PLAINTEXT_LISTENER
            value: "no"
        provisioning:
          enabled: true
          auth:
            tls:
              type: jks
              passwordsSecret: kafka-jks-password
              certificatesSecret: kafka-jks
              keystore: kafka.keystore.jks
              truststore: kafka.truststore.jks
          topics:
            - name: stalker.jobs.findings
              partitions: 1
              replicationFactor: 1
              config:
                max.message.bytes: 64000
                flush.messages: 1
            - name: stalker.jobs.requests
              partitions: 1
              replicationFactor: 1
              config:
                max.message.bytes: 64000
                flush.messages: 1
            - name: stalker.jobs.logs
              partitions: 1
              replicationFactor: 1
              config:
                max.message.bytes: 64000
                flush.messages: 1
          extraProvisioningCommands: # Setting the ACLs
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:flow-manager --operation read --operation write --operation describe --topic stalker.jobs.findings"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:flow-manager --operation write --operation describe --topic stalker.jobs.requests"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:flow-manager --operation read --operation describe --topic stalker.jobs.logs"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:orchestrator --operation write --operation describe --topic stalker.jobs.findings"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:orchestrator --operation read --operation describe --topic stalker.jobs.requests"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:orchestrator --operation write --operation describe --topic stalker.jobs.logs"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:flow-manager --operation read --operation describe --group flow-manager"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:flow-manager --operation read --operation describe --group flow-manager-job-logs"
            - "/opt/bitnami/kafka/bin/kafka-acls.sh --bootstrap-server $KAFKA_SERVICE --command-config $CLIENT_CONF --add --allow-principal User:orchestrator --operation read --operation describe --group stalker"

  orchestrator-service-account:
    kubectl:
      manifests:
        - orchestrator/orchestrator-service-account.yml

  orchestrator:
    helm:
      values:
        serviceAccountName: orchestrator-service-account
        containers:
          - env:
              - name: JobsQueue__QueueUri
                value: ${KAFKA_URI}
              - name: SECRET_PRIVATE_RSA_KEY
                value: ${SECRET_PRIVATE_RSA_KEY}
              - name: ORCHESTRATOR_KAFKA_KEY_PASSWORD
                value: ${ORCHESTRATOR_KAFKA_KEY_PASSWORD}

            image: orchestrator
            name: orchestrator-container

        service:
          ports:
            - containerPort: 5135
              port: 5135
              protocol: TCP

  namespaces:
    kubectl:
      manifests:
        - jobs/namespace.yml

  resource-quotas:
    kubectl:
      manifests:
        - jobs/${RESOURCE_QUOTA}

  network-policies:
    kubectl:
      manifests:
        - network-policy.yml
        - stalker_ui/${NETWORK_POLICY}
        - database/${NETWORK_POLICY}
        - flow_manager/${NETWORK_POLICY}
        - cron_service/${NETWORK_POLICY}
        - queue/${NETWORK_POLICY}
        - orchestrator/${NETWORK_POLICY}
        - jobs/${NETWORK_POLICY}

dev:
  mongo:
    labelSelector:
      statefulset.kubernetes.io/pod-name: mongo-mongodb-0
    ports:
      - port: "27017"

  mongo-prod:
    labelSelector:
      statefulset.kubernetes.io/pod-name: mongo-mongodb-0

  kafka:
    labelSelector:
      app.kubernetes.io/instance: kafka

  flow-manager:
    labelSelector:
      app.kubernetes.io/component: flow-manager
    container: flow-manager-container
    logs:
      enabled: true
    sync:
      - path: ./flow_manager/src/:/app/src/
        disableDownload: true
    restartHelper:
      inject: false
    ports:
      - port: "3000"

  flow-manager-prod:
    labelSelector:
      app.kubernetes.io/component: flow-manager
    container: flow-manager-container
    logs:
      enabled: true
    restartHelper:
      inject: false

  flow-manager-e2e:
    labelSelector:
      app.kubernetes.io/component: flow-manager-e2e
    container: flow-manager-container-e2e
    logs:
      enabled: true
    sync:
      - path: ./flow_manager/src/:/app/src/
        disableDownload: true
      - path: ./flow_manager/test/:/app/test/
        disableDownload: true
    restartHelper:
      inject: false
    ports:
      - port: "9229"

  cron-service:
    labelSelector:
      app.kubernetes.io/component: cron-service
    container: cron-service-container
    logs:
      enabled: true
    sync:
      - path: ./cron_service/src/:/app/src/
        disableDownload: true
    restartHelper:
      inject: false
    ports:
      - port: "3001:3000"

  cron-service-prod:
    labelSelector:
      app.kubernetes.io/component: cron-service
    container: cron-service-container
    restartHelper:
      inject: false

  ui:
    labelSelector:
      app.kubernetes.io/component: ui
    container: ui-container
    sync:
      - path: ./stalker_ui/src/:/app/src/
        disableDownload: true
    restartHelper:
      inject: false
    ports:
      - port: "4200"

  ui-prod:
    labelSelector:
      app.kubernetes.io/component: ui-prod
    container: ui-container
    restartHelper:
      inject: false
    ports:
      - port: "8000:80"
    resources:
      limits:
        memory: 1Gi

  orchestrator:
    labelSelector:
      app.kubernetes.io/component: orchestrator
    container: orchestrator-container
    sync:
      - path: ./orchestrator/:/app/
        disableDownload: true
        excludePaths:
          - Orchestrator/bin
          - Orchestrator/obj
    restartHelper:
      inject: false
    ports:
      - port: "5135"

  orchestrator-prod:
    labelSelector:
      app.kubernetes.io/component: orchestrator
    container: orchestrator-container
    restartHelper:
      inject: false

hooks:
  - events:
      - after:deploy:mongo
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/component: mongodb
      containerName: mongo-container

  - events:
      - after:deploy:flow-manager
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/component: flow-manager
      containerName: flow-manager-container

  - events:
      - after:deploy:cron-service
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/component: cron-service
      containerName: cron-service-container

  - events:
      - after:deploy:ui
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/component: ui
      containerName: ui-container

  - events:
      - after:deploy:ui-prod
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/component: ui-prod
      containerName: ui-container

  - events:
      - after:deploy:orchestrator
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/component: orchestrator
      containerName: orchestrator-container

  - events:
      - after:deploy:kafka
    wait:
      running: true
      terminatedWithCode: 0
    container:
      labelSelector:
        app.kubernetes.io/instance: kafka
      containerName: kafka-container
