name: Http(s) server port check
finding: PortFinding
triggerInterval: 86400
job:
  name: HttpServerCheckJob
  parameters:
    - name: targetIp
      value: ${ip}
    - name: ports
      value:
        - ${port}
conditions:
  - lhs: ${protocol}
    operator: equals
    rhs: tcp
